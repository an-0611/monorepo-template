<!-- pnpm install          # 安裝所有依賴
pnpm dev              # 啟動 apps/web
pnpm --filter ui dev  # 開發元件庫 (storybook 可另加)
pnpm build            # 打包所有 apps
pnpm lint             # ESLint + Prettier
pnpm test             # Vitest -->
